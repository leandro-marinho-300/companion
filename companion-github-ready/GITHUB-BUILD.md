# GitHub-ready build

This copy already includes the GitHub Actions workflow that builds the Windows NSIS installer.

When these files are committed to the `main` branch, GitHub Actions starts a Windows build automatically.
The resulting installer is uploaded as the workflow artifact `Companion-Windows-Installer`.
